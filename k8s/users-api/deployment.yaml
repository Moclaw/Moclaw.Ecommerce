apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: ecommerce
  labels:
    app: users-api
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
        version: v1
    spec:
      containers:
      - name: users-api
        image: moclaw/ecom-users-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 8081
          name: https
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:8080;https://+:8081"
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: users-api-secrets
              key: connection-string
        - name: JwtOptions__Secret
          valueFrom:
            secretKeyRef:
              name: users-api-secrets
              key: jwt-secret
        - name: JwtOptions__Issuer
          value: "ecom-users-api"
        - name: JwtOptions__Audience
          value: "ecom-clients"
        - name: JwtOptions__ExpiryMinutes
          value: "60"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
