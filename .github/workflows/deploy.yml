name: CI/CD Pipeline - Ecommerce Platform

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  DOTNET_VERSION: '9.0'
  REGISTRY_URL: ${{ secrets.REGISTRY_URL }}
  REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
  REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
  IMAGE_CORE: moclaw/ecom-core-api
  IMAGE_USERS: moclaw/ecom-users-api
  KUBE_NAMESPACE: ecommerce
  MONITORING_NAMESPACE: monitoring

jobs:
  build-and-test:
    name: Build & Test
    runs-on: self-hosted
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      core-image: ${{ steps.meta.outputs.core-image }}
      users-image: ${{ steps.meta.outputs.users-image }}
    
    steps:
      - name: Reset permissions (self-hosted only)
        run: |
          sudo chown -R $USER:$USER $GITHUB_WORKSPACE
          sudo chmod -R 755 $GITHUB_WORKSPACE

      - name: 🧹 Pre-clean workspace
        run: |
          echo "Cleaning workspace and stopping services..."
          docker system prune -af --volumes || true
          pkill -f "dotnet\|docker-compose" || true
          fuser -k 5301/tcp 5302/tcp 9090/tcp 3000/tcp || true
          rm -rf ./monitoring ./bin ./obj || true

      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔧 Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
        env:
          DOTNET_INSTALL_DIR: ${{ github.workspace }}/.dotnet

      - name: 🔧 Add .NET to PATH
        run: |
          echo "${{ github.workspace }}/.dotnet" >> $GITHUB_PATH
          export PATH="${{ github.workspace }}/.dotnet:$PATH"
          dotnet --version

      - name: 📦 Cache NuGet packages
        uses: actions/cache@v3
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: ${{ runner.os }}-nuget-

      - name: 🔨 Restore dependencies
        run: |
          echo "Restoring NuGet packages..."
          dotnet restore Moclaw.Ecommerce.sln --verbosity normal

      - name: 🏗️ Build solution
        run: |
          echo "Building solution in Release mode..."
          dotnet build Moclaw.Ecommerce.sln --configuration Release --no-restore --verbosity normal

      - name: 🏷️ Generate metadata
        id: meta
        run: |
          TAG="${{ github.sha }}"
          echo "tags=${TAG}" >> $GITHUB_OUTPUT
          echo "core-image=${{ env.IMAGE_CORE }}:${TAG}" >> $GITHUB_OUTPUT
          echo "users-image=${{ env.IMAGE_USERS }}:${TAG}" >> $GITHUB_OUTPUT

  build-images:
    name: Build Docker Images
    runs-on: self-hosted
    needs: build-and-test
    strategy:
      matrix:
        service:
          - name: core-api
            dockerfile: Ecom.Core/src/Ecom.Core.API/Dockerfile
            image: ${{ needs.build-and-test.outputs.core-image }}
          - name: users-api
            dockerfile: Ecom.Users/src/Ecom.Users.API/Dockerfile
            image: ${{ needs.build-and-test.outputs.users-image }}
      fail-fast: false

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Login to Docker registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY_URL }}
          username: ${{ env.REGISTRY_USERNAME }}
          password: ${{ env.REGISTRY_PASSWORD }}

      - name: 🏗️ Build and push ${{ matrix.service.name }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.service.dockerfile }}
          push: true
          tags: ${{ matrix.service.image }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  deploy-minikube:
    name: Deploy to Minikube
    runs-on: self-hosted
    needs: [build-and-test, build-images]
    if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🚀 Setup Minikube
        uses: medyagh/setup-minikube@latest
        with:
          minikube-version: 'latest'
          driver: docker
          kubernetes-version: 'v1.28.3'
          memory: 6144
          cpus: 4
          start: false

      - name: ▶️ Start Minikube cluster
        run: |
          echo "Starting Minikube cluster..."
          minikube delete || true
          minikube start \
            --driver=docker \
            --kubernetes-version=v1.28.3 \
            --memory=6144 \
            --cpus=4 \
            --disk-size=20g \
            --wait=all
          
          # Enable addons
          minikube addons enable ingress
          minikube addons enable metrics-server
          
          # Wait for cluster to be ready
          kubectl wait --for=condition=Ready nodes --all --timeout=300s
          kubectl cluster-info

      - name: 🔄 Load Docker images
        run: |
          echo "Loading images into Minikube..."
          minikube image load ${{ needs.build-and-test.outputs.core-image }}
          minikube image load ${{ needs.build-and-test.outputs.users-image }}
          
          # Verify images are loaded
          minikube image ls | grep moclaw

      - name: 🏗️ Deploy Kubernetes resources
        run: |
          echo "Deploying Kubernetes manifests..."
          
          # Create namespaces
          kubectl create namespace ${{ env.KUBE_NAMESPACE }} --dry-run=client -o yaml | kubectl apply -f -
          
          # Apply base manifests
          kubectl apply -f k8s/namespace.yaml
          kubectl apply -f k8s/secrets.yaml
          kubectl apply -f k8s/configmap.yaml
          kubectl apply -f k8s/postgres/deployment.yaml
          kubectl apply -f k8s/core-api/deployment.yaml
          kubectl apply -f k8s/core-api/service.yaml
          kubectl apply -f k8s/users-api/deployment.yaml
          kubectl apply -f k8s/users-api/service.yaml
          kubectl apply -f k8s/ingress.yaml
          
          # Update image tags
          kubectl set image deployment/core-api \
            core-api=${{ needs.build-and-test.outputs.core-image }} \
            -n ${{ env.KUBE_NAMESPACE }}
          
          kubectl set image deployment/users-api \
            users-api=${{ needs.build-and-test.outputs.users-image }} \
            -n ${{ env.KUBE_NAMESPACE }}

      - name: ⏳ Wait for deployments
        run: |
          echo "Waiting for deployments to be ready..."
          kubectl rollout status deployment/postgres -n ${{ env.KUBE_NAMESPACE }} --timeout=300s
          kubectl rollout status deployment/core-api -n ${{ env.KUBE_NAMESPACE }} --timeout=600s
          kubectl rollout status deployment/users-api -n ${{ env.KUBE_NAMESPACE }} --timeout=600s

      - name: 🩺 Health checks
        run: |
          echo "Performing health checks..."
          
          # Check Kubernetes services
          kubectl get pods -n ${{ env.KUBE_NAMESPACE }}
          kubectl get services -n ${{ env.KUBE_NAMESPACE }}
          
          # Get Minikube IP and NodePort services
          MINIKUBE_IP=$(minikube ip)
          
          # Health endpoint checks with NodePort
          max_attempts=10
          attempt=1
          
          while [ $attempt -le $max_attempts ]; do
            echo "Health check attempt $attempt/$max_attempts"
            
            if curl -f -s http://$MINIKUBE_IP:30301/health > /dev/null && \
               curl -f -s http://$MINIKUBE_IP:30302/health > /dev/null; then
              echo "✅ All services are healthy!"
              break
            fi
            
            if [ $attempt -eq $max_attempts ]; then
              echo "❌ Health checks failed after $max_attempts attempts"
              kubectl logs -l app=core-api -n ${{ env.KUBE_NAMESPACE }} --tail=50
              kubectl logs -l app=users-api -n ${{ env.KUBE_NAMESPACE }} --tail=50
              exit 1
            fi
            
            echo "Services not ready yet, waiting..."
            sleep 15
            attempt=$((attempt+1))
          done

      - name: 🧪 Integration tests
        run: |
          echo "Running integration tests..."
          MINIKUBE_IP=$(minikube ip)
          
          # Basic API tests using NodePort
          echo "Testing Core API..."
          curl -f http://$MINIKUBE_IP:30301/health || exit 1
          
          echo "Testing Users API..."
          curl -f http://$MINIKUBE_IP:30302/health || exit 1
          
          echo "✅ Integration tests passed!"

      - name: 📊 Deployment summary
        run: |
          echo "==================== DEPLOYMENT SUMMARY ===================="
          MINIKUBE_IP=$(minikube ip)
          
          echo "🌐 Service URLs:"
          echo "  • Core API:              http://$MINIKUBE_IP:30301"
          echo "  • Users API:             http://$MINIKUBE_IP:30302"
          echo "  • Core API Health:       http://$MINIKUBE_IP:30301/health"
          echo "  • Users API Health:      http://$MINIKUBE_IP:30302/health"
          echo "  • Core API Swagger:      http://$MINIKUBE_IP:30301/docs"
          echo "  • Users API Swagger:     http://$MINIKUBE_IP:30302/docs"
          echo ""
          
          echo "☸️ Kubernetes Services:"
          kubectl get services -n ${{ env.KUBE_NAMESPACE }} -o wide
          echo ""
          
          echo "🏷️ Image Tags:"
          echo "  • Core API:  ${{ needs.build-and-test.outputs.core-image }}"
          echo "  • Users API: ${{ needs.build-and-test.outputs.users-image }}"
          echo ""
          
          echo "📦 Pods Status:"
          kubectl get pods -n ${{ env.KUBE_NAMESPACE }} -o wide
          echo ""
          
          echo "📊 Resource Usage:"
          kubectl top nodes || echo "Metrics not available yet"
          kubectl top pods -n ${{ env.KUBE_NAMESPACE }} || echo "Pod metrics not available yet"
          echo "==========================================================="

      - name: 🧹 Cleanup on failure
        if: failure()
        run: |
          echo "Cleaning up after failure..."
          kubectl delete namespace ${{ env.KUBE_NAMESPACE }} --ignore-not-found=true
          minikube delete || true
